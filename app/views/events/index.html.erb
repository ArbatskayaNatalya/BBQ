<div class="container">
  <h1 class="mt-3"><%= t('titles.events') %></h1>

  <div class="mb-3">
    <%= link_to t('buttons.new_event'), new_event_path, class: 'btn btn-success' %>
  </div>

  <div class="row mb-2">
    <%= render partial: 'events/event', collection: @events %>
  </div>
</div>
