<div class="container">
  <div class="row mb-4">
    <div class="col-md-6">
      <h1 class="mt-3"><%= t('titles.edit_event') %></h1>
      <%= render 'form' %>
      <%= link_to t('buttons.delete'), event_path(@event), data: { confirm: t('confirm.agree_event') }, method: :delete, class: 'btn btn-secondary mt-5' %>
    </div>
  </div>
</div>
